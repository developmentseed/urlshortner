# Devseed URL Shortener

## Installation

    $ npm install

### Adding a new link

To create `https://ds.io/google` run:

    $ ./add.js https://www.google.com --name google

### Deploying a new verison

Upload to cloudformation.yml to AWS' CF service.
